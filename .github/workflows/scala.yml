name: "AWS account test"
on:
  push: 
jobs:
  "scala-job":
     runs-on: ubuntu-latest
     steps:
        - name: check directory 
          run: |
            echo $PATH
            ls /opt/hostedtoolcache/
            which sbt
       
     
